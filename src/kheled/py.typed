# 1. Create and activate a virtual environment
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate

# 2. Install kheled with dev dependencies in editable mode
pip install -e ".[dev]"

# 3. Run test suite
pytest